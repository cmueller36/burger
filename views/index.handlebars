<div class="container">
    <div class="row">
        <div id="header" class="col-md-12">
            <h1>Welcome to Good Burgers</h1>
            <br>
            <img id="inANDout" src="/assets/img/in-n-out.png">
        </div>
    </div>
    <br>
    <br>
    <div class="row">
        <div class="col-md-6">
            <ul class="list-group">

                {{#each burgers}} {{#unless this.devoured}}
                <li class="list-group-item">{{id}}. {{burger_name}}</li>
                <button type="submit" data-id="{{id}}" data-devour="{{devoured}}" class="devour-burger btn btn-dark">
                    Devour</button>
                {{/unless}} {{/each}}

            </ul>
        </div>
        <div class="col-md-6">
                 {{#each burgers}} {{#if this.devoured}}
                <li class="list-group-item">{{burger_name}}</li>
                {{/if}} {{/each}}
        </div>
    </div>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            <form>
                <div class="form-group">
                    <label for="burgerText">Order a Burger</label>
                    <textarea class="form-control" id="burgerText" rows="3"></textarea>
                </div>
                <button id="newBurger" type="submit" class="btn btn-dark">Submit</button>
            </form>
        </div>
        <div class="col-md-4"></div>
    </div>
</div>